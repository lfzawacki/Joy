#ifndef _STRING_EX_H
#define _STRING_EX_H

#include <string>
#include <vector>
#include <iostream>
#include <fstream>

#define STREAM_SIZE(s,i)    
						(s).seekg(0,ios::end); 
						(i) = (s).tellg(); 
						(s).seekg(0,ios::beg)

using namespace std;

wstring  astows (string str);
string wstoas (wstring wstr);
wstring atows(const char *c_str);
string& stripExtraSpaces(string& t);

string& stripComments(string& conteudo);
vector<string> splitString(string& str, string separador);
string& trimSpaces( string& str);
string readFile(string name);
string vectorToString(vector<string>& vec);
#endif
